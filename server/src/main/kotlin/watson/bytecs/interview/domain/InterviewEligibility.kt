package watson.bytecs.interview.domain

/**
 * 면접 후보 승급 임계(DI8) — 개념 숙련도 레벨이 이 값 이상이면 그 개념이 면접 세션의 후보가 된다.
 *
 * 후보 산정(면접 세션 시작 시 승급 후보 선정)과 정답 시 **신규 승급 판정**(주관식 정답으로 개념이 처음 후보가 되는 순간, DI9)이
 * **같은 기준**을 쓰도록 임계를 한곳에 둔다 — 두 판정이 갈리면 "홈 카드엔 후보로 잡히는데 정답 순간엔 안 열림" 같은 불일치가 난다.
 */
object InterviewEligibility {
    /** DI8: 숙련도 레벨 ≥ 1 (무도움 정답 1회로 도달하는 최소 정착선). */
    const val MASTERY_LEVEL = 1
}
